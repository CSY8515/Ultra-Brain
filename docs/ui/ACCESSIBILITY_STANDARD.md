# Accessibility Standard

All destinations use semantic links or buttons with Korean-first accessible names. Keyboard focus communicates the same destination and state as hover. Touch targets are at least 40px in the shell and 44px on narrow screens.

Status is expressed with text and shape in addition to color. System reduced-motion and the user motion setting remove nonessential transitions. The world supports reflow at mobile widths without horizontal overflow.
