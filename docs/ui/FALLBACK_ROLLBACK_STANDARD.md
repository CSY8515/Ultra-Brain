# Fallback and Rollback Standard

Invalid settings fall back to the official profile. A successful save creates a rollback point containing the previous validated preference and revision. Rollback creates a new revision rather than erasing history.

Downstream failure must retain the prior target theme, report the affected scope, and never weaken a lock or compatibility requirement.
