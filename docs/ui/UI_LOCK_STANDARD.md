# UI Lock Standard

Locks protect a scope or token group from higher-level theme changes. Supported lock identities include global, OS Ecosystem, project, module, feature, background, layout, component style, and user customization.

The v0.8 control exposes OS Ecosystem theme lock. Impact reporting must explicitly show when propagation is blocked; a lock is never silently bypassed.
