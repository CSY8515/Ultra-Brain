# Theme Propagation

Flow: User change → Ultra Brain profile → validation → affected-scope report → preview → approval → OS Ecosystem compatibility boundary → downstream adapters → result → rollback point.

v0.8 implements resolution, preview, impact reporting, lock evaluation, device-local save, and rollback. It does not introduce network transport or force changes into independent applications.
