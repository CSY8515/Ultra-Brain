# UI Registry

The registry records theme ID, version, owner, scope, target hierarchy, compatible project, required contract version, override, lock, status, fallback, and rollback reference.

The official profile is `ultra-brain-official`. Compatible target `os-ecosystem` requires `ultra-brain.ui/v1` and interface `1.0`. Registry presence does not authorize runtime mutation.
